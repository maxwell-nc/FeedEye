<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/selector_press_background_light_white"
    android:paddingTop="6dp" >

    <LinearLayout
        android:id="@+id/rl_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:gravity="center_vertical"
        android:orientation="horizontal" >

        <pres.nc.maxwell.feedeye.view.LayoutImageView
            android:id="@+id/iv_item_feed_pic"
            android:layout_width="16dp"
            android:layout_height="match_parent"
            android:layout_marginLeft="10dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_launcher" />

        <TextView
            android:id="@+id/tv_item_feed_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="5dp"
            android:layout_marginRight="5dp"
            android:layout_weight="1"
            android:ellipsize="middle"
            android:singleLine="true"
            android:text="订阅标题"
            android:textColor="@drawable/selector_press_text_color"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/tv_item_feed_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="10dp"
            android:gravity="right"
            android:text="09/12 上午"
            android:textColor="@drawable/selector_press_text_color_grey"
            android:textSize="10sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_item_feed_preview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/rl_container"
        android:layout_marginBottom="10dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_marginTop="5dp"
        android:ellipsize="end"
        android:maxLines="3"
        android:text="内容预览：这是一个listView的预览文本，用于测试时使用,这是一个listView的预览文本，用于测试时使用"
        android:textColor="@drawable/selector_press_text_color_grey"
        android:textSize="14sp" />

    <View
        android:layout_below="@id/tv_item_feed_preview"
        android:layout_marginBottom="0.1dp"
        style="@style/hr_line" />

</RelativeLayout>